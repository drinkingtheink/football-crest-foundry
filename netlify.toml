[build]
  command = "npm run build"
  publish = "dist"

# The share-og edge function is registered via its own inline `config.path`
# (netlify/edge-functions/share-og.js) — no [[edge_functions]] entry needed.
